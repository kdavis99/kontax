<%= form_for([@job, @update]) do |f| %>
  <% if @update.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(update.errors.count, "error") %> prohibited this update from being saved:</h2>

      <ul>
      <% update.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="row" id="form_for">
      <div class="large-12 columns select-date-wrapper">
        <%= f.date_select(:u_date, options = {order: [:month, :day, :year]}, html_options = { class: 'select-date'}) %>
      </div>
    </div>
  </div>

  <div class="field">
     <%= f.text_area :note, placeholder: "Additional Notes", class: "col-md-4 control-label", :rows => "4", :cols => "11" %>
  </div>
  <br><br>
  <br><br>
  <br><br>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
